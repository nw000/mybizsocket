apply plugin: 'java'

sourceCompatibility = 1.7

dependencies {
    compile 'com.squareup.okio:okio:1.9.0'
    compile project(':bizsocket-common')

    testCompile group: 'junit', name: 'junit', version: '4.11'
}
